package main

import "testing"

func TestMain_runs(t *testing.T) {
	// Smoke test — verify the package compiles and main exists.
	// Replace with real tests as the project grows.
	t.Log("package compiles")
}
